
//TDZ zone starts from the start of the block until the variable is declared and initialized.
console.log(status_code);

//TDZ zone ends after the variable is declared and initialized.
let status_code=200;